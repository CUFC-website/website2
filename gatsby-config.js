/**
 * Gatsby configuration.
 * Set pathPrefix only if you host the site in a subdirectory, e.g. example.com/site/.
 */
module.exports = {
  siteMetadata: {
    title: `Gatsby Apache Starter`,
    description: `A minimal Gatsby website ready for Apache static hosting.`,
    siteUrl: `https://example.com`,
  },
  plugins: [],
}
